#include <stdio.h>  

int main() {
    int a = 7;       // Declare and initialize first number
    int b = 5;       // Declare and initialize second number

    // Multiply the two numbers and store the result
    int product = a * b;  
    
    // Print the result
    printf("Multiplication result: %d\n", product);  

    return 0; 
}
